import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'codeswag-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
